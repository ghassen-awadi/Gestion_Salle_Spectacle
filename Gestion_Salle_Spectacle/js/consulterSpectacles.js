$(document).ready(function(){
	$("")
		/*	$(".btn-info").click(function() {
		   		 var row = $(this).closest("tr");    // Find the row
		   		 var text = row.find(".idSpectacle").text(); // Find the text
				if (navigator.cookieEnabled)
				{
					document.cookie = "idSpectacle="+text;
					window.location.replace("http://localhost/gestion_salle_spectacle/visiteur/detail_spectacle.php");
					//window.location.replace("http://localhost/gestion_salle_spectacle/test.php");
				}
				else
				{
					window.location.replace("detail_spectacle.php?idSpectacle="+text);
				}
	});
				$(".btn-success").click(function(){
				window.location.replace("http://localhost/gestion_salle_spectacle/login.php");
			});*/
});